def add(x, y=100) -> int:
    """
    Add two objects together.

    Args:
        x: The first object.
        y: The second object. Defaults to 100.
    Returns:
        int: The sum of the two objects.
    """
    return x + y
